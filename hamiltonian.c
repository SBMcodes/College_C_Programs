#include<stdio.h>

// What is a hamiltonian cycle?
// Hamiltonian cycle is a cycle in a graph where from starting vertices we traverse all vertices without repeating
// the any vertice and it should terminate at starting vertice

int main(){
	
	return 0;
}
